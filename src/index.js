const { ChatComponent } = require('./components/ChatComponent');
const LicenseService = require('./services/LicenseService');

module.exports = {
  NexionChat: ChatComponent,
  LicenseService: LicenseService
};